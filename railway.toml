[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node generate.js"
restartPolicyType = "NEVER"

[[cronJobs]]
# Runs at 4:00 AM UTC = 12:00 AM Eastern (midnight Ontario)
# Stories ready fresh every morning when users wake up
schedule = "0 4 * * *"
command = "node generate.js"
